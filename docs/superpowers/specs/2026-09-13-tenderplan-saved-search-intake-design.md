# Автоматический захват новых закупок из сохранённых ключей TenderPlan

**Дата:** 2026-09-13

**Статус:** согласованный дизайн, реализация не начата

**Ветка:** `codex/tenderplan-saved-search-intake`

## 1. Цель

Каждые 10 минут проверять один или несколько сохранённых ключей TenderPlan и
автоматически запускать существующий агентский анализ только для закупок,
которые впервые появились в выдаче после включения соответствующего ключа.

Закупки, уже находящиеся в конкретном сохранённом ключе во время его
первоначальной инициализации, не анализируются.

После обнаружения новой закупки используется существующая цепочка:

```text
TenderPlan saved keys
→ Intake Resume
→ TenderPlan Orchestrator
→ Подготовка документов
→ Агентский анализ Codex
→ 27 итоговых результатов
→ Финализация
→ PDF-отчёт
```

## 2. Проверенное текущее состояние

Read-only проверка live n8n от 2026-09-13 подтвердила:

- `[CODEX CANDIDATE] TENDER — TenderPlan Mark Intake` активен;
- расписание уже выполняется раз в 10 минут;
- workflow получает закупки по метке через
  `GET /api/tenders/v2/getlist?type=1&id=<mark_id>`;
- каждая найденная закупка передаётся в активный `Intake Resume`;
- `Intake Resume` использует `source_event_key` и таблицу
  `tender_analysis_intake_events` для идемпотентной обработки событий;
- дополнительная защита не создаёт второй незавершённый или уже завершённый
  `analysis_run` для того же `tender_id`.

Текущая repository-документация оркестратора описывает более старый ручной вход
и не полностью соответствует live n8n. Для этой спецификации фактическим
источником текущего поведения считается live workflow.

## 3. TenderPlan API

Официальная документация TenderPlan определяет для
`GET /api/tenders/v2/getlist` следующие режимы:

- `type=0` — получить закупки по сохранённому ключу;
- `type=1` — получить закупки по метке;
- `id` — идентификатор соответствующего ключа или метки;
- `page` — номер страницы выдачи.

Метод требует право `relations:read`.

Получение списка сохранённых ключей через `GET /api/keys/getall` требует
`keys:read`. Оно нужно только для автоматического поиска ID ключа по названию.
Если ID сохранённого ключа известен заранее, runtime использует только
`relations:read`.

Механизм TenderPlan Cursor API не используется: он помечен deprecated и не
поддерживает фильтрацию непосредственно по сохранённому пользовательскому
ключу.

Официальная документация: <https://tenderplan.ru/api/doc/>.

## 4. Выбранный подход

Создать отдельный небольшой входной workflow и сохранить существующий вход по
метке без изменений.

Рабочее техническое название:

```text
TENDER — TenderPlan Saved Key Intake
```

Человекопонятное назначение:

```text
Получение новых закупок из сохранённого поиска TenderPlan
```

Такой вариант:

- не смешивает ручную метку и автоматический поиск;
- не меняет основную цепочку анализа;
- использует существующие механизмы идемпотентности;
- позволяет независимо включать и отключать каждый автоматический поиск;
- оставляет полный audit trail обнаружения закупки.

Один workflow обслуживает список ключей. Создавать отдельный workflow для
каждого сохранённого поиска не требуется.

## 5. Первый запуск и момент включения

### 5.1. Определение момента включения

Каждый ключ считается включённым после успешного завершения его первоначальной
инициализации и появления отдельного baseline marker в PostgreSQL.

Это устраняет неоднозначность для закупок, которые появляются во время
первоначального чтения нескольких страниц TenderPlan.

### 5.2. Порядок первоначальной инициализации

Новый workflow сначала остаётся неактивным и один раз запускается вручную для
всех ключей, включённых в конфигурации.

Для каждого ключа он должен:

1. Получить все страницы выдачи этого сохранённого ключа.
2. Проверить структуру всех ответов.
3. Собрать уникальные `tender_id`.
4. Одной атомарной DB-операцией зарегистрировать их как baseline-события со
   статусом `completed` и действием `baseline_existing_skipped`.
5. В той же операции создать baseline marker для этого ключа.
6. Вернуть идентификатор ключа и количество закупок, сохранённых в baseline.
7. Не вызывать `Intake Resume` и не создавать `analysis_run`.

Если для ключа не удалось получить хотя бы одну страницу или проверить ответ,
DB-операция для этого ключа не выполняется и его baseline marker не создаётся.
Успешно инициализированные ключи не откатываются, но расписание первоначально не
активируется, пока baseline marker отсутствует хотя бы у одного включённого
ключа.

После проверки результата первоначального запуска workflow активируется по
расписанию.

Если новый ключ добавляется позднее, первый цикл только создаёт для него
baseline и не запускает анализ найденных в этом цикле закупок. Со следующего
цикла ключ работает в обычном режиме. Другие уже инициализированные ключи на это
время не останавливаются.

### 5.3. Идентификаторы baseline

Для baseline и рабочих событий используется существующая таблица
`tender_analysis_intake_events`. Новая таблица не создаётся.

Baseline marker:

```text
source = tenderplan
event_key = tenderplan:key:<saved_key_id>:baseline:v1
event_type = key_baseline_completed
tender_id = baseline:<saved_key_id>
status = completed
action = baseline_initialized
```

Синтетическое `tender_id` marker обязательно, потому что колонка
`tender_analysis_intake_events.tender_id` имеет `NOT NULL`. Оно никогда не
передаётся в Orchestrator и не является идентификатором закупки.

Baseline-событие конкретной закупки использует тот же event key, который позже
использовался бы для её запуска:

```text
tenderplan:key:<saved_key_id>:tender:<tender_id>
```

Поэтому закупка из первоначального списка не сможет случайно перейти в анализ
после перезапуска или повторной инициализации.

## 6. Регулярная проверка

После инициализации workflow каждые 10 минут для каждого включённого ключа
выполняет:

1. Читает отдельный baseline marker ключа.
2. Если marker отсутствует, выполняет для этого ключа только baseline-процедуру
   из раздела 5 и не запускает анализы.
3. Если marker существует, получает все страницы выдачи ключа с параметрами:

   ```text
   type = 0
   id = <saved_key_id>
   page = 0, 1, 2, ...
   ```

4. Завершает пагинацию только после корректного признака конца выдачи.
5. Проверяет, что каждый найденный объект содержит корректный `tender_id`.
6. Удаляет дубли `tender_id` внутри ответа этого ключа.
7. Одним запросом получает event keys со статусами `processing` и `completed`.
8. Передаёт в `Intake Resume` закупки без такого события, включая события со
   статусом `failed`, которые существующая логика умеет claim-нуть повторно.

Чтение всех страниц одного ключа обязательно. Частичная выдача не должна
приводить к обновлению состояния или запуску части найденных по этому ключу
закупок.

Ключи обрабатываются как независимые единицы. Ошибка одного ключа не откатывает
успешную обработку других, но обязательно отражается как ошибка выполнения и не
подавляется.

До runtime-подтверждения фактической сортировки TenderPlan запрещено
останавливать пагинацию после первой уже известной закупки: такой алгоритм может
пропустить новые элементы на последующих страницах.

Пагинация имеет конечный защитный предел. Достижение предела является ошибкой,
а не успешным частичным результатом. Точное значение определяется после
read-only probe настроенных сохранённых ключей.

## 7. Контракт передачи в Intake Resume

Для каждой новой закупки передаётся:

```json
{
  "trigger_kind": "tenderplan_key",
  "source_event_key": "tenderplan:key:<saved_key_id>:tender:<tender_id>",
  "tender_id": "<tender_id>",
  "analysis_run_id": "",
  "manual_override": false,
  "observed_at": "<ISO-8601 timestamp with timezone>"
}
```

Вызов выполняется отдельно для каждой закупки, без ожидания завершения полного
анализа.

## 8. Минимальные изменения существующей входной цепочки

`Intake Resume` должен принять новый `trigger_kind = tenderplan_key`.

### 8.1. `Validate Intake Input`

Для `tenderplan_key` действуют те же identity-ограничения, что для
`tenderplan_mark`:

- `tender_id` обязателен;
- `analysis_run_id` запрещён;
- `manual_override` должен быть `false`;
- `intent = automatic`;
- `run_authoritative = false`;
- `source = tenderplan`.

### 8.2. `Prepare Event Identity`

Добавляется отображение:

```text
trigger_kind = tenderplan_key
→ source = tenderplan
→ event_type = key_match_added
```

Остальная логика `Intake Resume` не меняется.

### 8.3. Входная проверка TenderPlan Orchestrator

`Call Intake Resume Orchestrator` передаёт `trigger_kind` дальше без
преобразования. Поэтому нода `Проверить вход Orchestrator` должна добавить
`tenderplan_key` в существующий allow-list допустимых автоматических входов.

Другой код Orchestrator, подготовка документов и создание `analysis_run` не
меняются.

### 8.4. CHECK-контракт intake ledger

Актуальная схема `tender_analysis_intake_events` разрешает только
`tenderplan_mark`, `recovery_scan` и `manual`. До первого события нового типа
нужно выполнить отдельную fail-closed migration, которая добавит
`tenderplan_key` только в CHECK колонки `trigger_kind`.

Migration не добавляет таблиц или колонок и не меняет status-контракт. Перед
заменой она обязана подтвердить точную текущую форму CHECK, а после замены —
точный набор из четырёх допустимых значений. Неизвестная форма constraint
должна откатить transaction.

## 9. Защита от повторов

Используются два уже существующих уровня защиты.

### 9.1. Уровень события

Уникальная пара `(source, event_key)` в
`tender_analysis_intake_events` гарантирует, что одна закупка одного сохранённого
ключа будет принята только один раз.

Одна закупка может одновременно попасть под несколько ключей и создать по
одному audit event для каждого ключа. Это ожидаемо: следующий уровень защиты
всё равно допускает только один анализ закупки.

### 9.2. Уровень запуска

`Resolve TenderPlan Runs` проверяет существующие запуски по сочетанию:

```text
source = tenderplan
tender_id = <tender_id>
```

Если закупка ранее уже анализировалась через метку, второй `analysis_run` не
создаётся, даже если событие сохранённого ключа имеет другой event key.

Одновременное обнаружение одной закупки несколькими ключами дополнительно
защищено существующим атомарным `INSERT ... ON CONFLICT (source, tender_id)` для
незавершённых запусков в Orchestrator.

Новый parser, validator или field-specific rule для этой функции не требуется.

## 10. Ошибки и восстановление

### TenderPlan временно недоступен

Execution завершается ошибкой без записи baseline или частичного набора
событий. Следующий плановый цикл снова читает полный список.

### Ответ имеет неизвестную структуру

Workflow завершается ошибкой. Неизвестный объект не пропускается молча.

### Ошибка одной страницы

Ни одна закупка из текущего цикла не передаётся дальше. Следующий цикл повторяет
полное чтение.

### HTTP 429

HTTP Request применяет ограниченный retry с задержкой. Если попытки исчерпаны,
execution завершается ошибкой и восстанавливается следующим циклом.

### Повторный или пересекающийся schedule execution

Оба execution могут увидеть один и тот же новый `tender_id`, но только один
сможет успешно claim-нуть уникальный intake event. Второй получает безопасный
no-op.

### Постоянная ошибка доступа

Ошибка не подавляется и видна как failed execution n8n. Внешнее уведомление об
ошибках не входит в эту задачу и подключается к общему каналу мониторинга после
выбора канала доставки.

## 11. Credentials и конфигурация

TenderPlan access token остаётся в существующем n8n Credential типа Header Auth.
Токен не хранится:

- в Code Node;
- в Set Node;
- в workflow JSON;
- в repository;
- в n8n Variables.

Идентификаторы ключей не являются секретами. Workflow содержит явный список
настроек следующего смысла:

```json
[
  {
    "saved_key_id": "<id>",
    "name": "<человекопонятное название>",
    "enabled": true
  }
]
```

Новый ключ добавляется одной записью в этот список. `enabled = false` позволяет
временно исключить ключ из опроса, не удаляя его конфигурацию и baseline.
Идентификатор и название каждого ключа сохраняются в заметке workflow, чтобы
оператор понимал, какие поиски подключены.

Перед реализацией проверяется, что credential имеет `relations:read`. Если ID
ключа нужно получить по названию через API, отдельно проверяется `keys:read`.

## 12. Затрагиваемые компоненты

### Создать

- один workflow `TENDER — TenderPlan Saved Key Intake`;
- repository export этого workflow;
- описание workflow;
- одну migration, расширяющую CHECK `trigger_kind` значением
  `tenderplan_key`;
- offline regression tests для контракта нормализации и baseline;
- runtime canary checklist.

### Минимально изменить

- `Intake Resume / Validate Intake Input`;
- `Intake Resume / Prepare Event Identity`;
- repository export и документацию `Intake Resume`.
- `TenderPlan Orchestrator / Проверить вход Orchestrator` — только allow-list
  `trigger_kind`;
- repository export и документацию Orchestrator.

### Не изменять

- вход по метке;
- ручную загрузку;
- остальную логику TenderPlan Orchestrator;
- подготовку документации и архивов;
- агентский runner;
- монитор агентского анализа;
- JSON-контракт 27 полей;
- финализацию;
- генерацию PDF;
- бизнес-семантику `FIELD_CATALOG.md`;
- все таблицы, колонки и индексы PostgreSQL; меняется только allow-list
  существующего CHECK `trigger_kind`.

## 13. Проверки

### Offline

1. Первый запуск одного ключа при 50 существующих закупках создаёт baseline из
   50 элементов и 0 анализов.
2. Повторная инициализация не меняет baseline и создаёт 0 анализов.
3. После baseline появляется один новый `tender_id` — формируется один входной
   объект `tenderplan_key`.
4. Следующий poll с тем же списком не формирует новый объект.
5. Несколько новых закупок формируют по одному объекту на каждую.
6. Дубли одной закупки на разных страницах схлопываются.
7. Неверный `tender_id` останавливает цикл.
8. Ошибка промежуточной страницы не допускает частичного dispatch.
9. Новый event key устойчив к повторному исполнению.
10. `Intake Resume` принимает `tenderplan_key` и отклоняет несовместимые identity
    поля.
11. TenderPlan Orchestrator принимает `tenderplan_key`, сохраняя остальные
    входные ограничения.
12. Event со статусом `failed` не подавляется предварительным фильтром и может
    быть повторно claim-нут `Intake Resume`.
13. Migration принимает точный прежний CHECK, добавляет только
    `tenderplan_key`, безопасно повторяется при уже обновлённом CHECK и
    отклоняет неизвестную форму constraint.
14. Два ключа инициализируются независимо и не смешивают event keys.
15. Одна закупка, найденная двумя ключами, создаёт не более одного
    `analysis_run`.
16. Добавленный позднее ключ на первом цикле создаёт только baseline, а уже
    работающие ключи продолжают обычную обработку.

### Runtime canary

1. Выполнить inactive probe всех настроенных сохранённых ключей.
2. Для каждого ключа зафиксировать структуру ответа, число страниц и критерий
   завершения пагинации.
3. Выполнить ручную baseline-инициализацию всех включённых ключей.
4. Подтвердить: старые закупки ни одного ключа не создали `analysis_run`.
5. Добавить одну контролируемую новую закупку в выдачу одного ключа либо
   дождаться её естественного появления.
6. Подтвердить один intake event и один analysis run.
7. Повторить poll и подтвердить отсутствие второго run.
8. Только после этого активировать расписание раз в 10 минут.

## 14. Критерии готовности

Функция считается готовой, когда:

- baseline успешно зафиксирован для каждого первоначально включённого ключа до
  активации расписания;
- ни одна закупка из baseline любого ключа не получила analysis run;
- новая закупка проходит весь существующий путь до PDF;
- повторные polls не создают дубли;
- закупка, совпавшая с несколькими ключами, получает не более одного
  analysis run;
- новый ключ можно добавить без создания отдельного workflow и без остановки
  уже настроенных ключей;
- ошибка или неполная пагинация не дают частичный dispatch;
- CHECK `tender_analysis_intake_events.trigger_kind` принимает
  `tenderplan_key` и сохраняет прежние три значения;
- токен TenderPlan отсутствует в export и repository;
- актуальные workflow exports и документация сохранены в проекте;
- runtime evidence первого полного canary зафиксирован.

## 15. Следующий шаг после утверждения спецификации

Составить пошаговый implementation plan:

1. read-only probe всех настроенных сохранённых ключей;
2. fail-closed migration для CHECK `trigger_kind`;
3. offline workflow export и тесты;
4. минимальное расширение `Intake Resume` и входного allow-list Orchestrator;
5. inactive live deployment;
6. baseline initialization;
7. один controlled canary;
8. активация расписания.
